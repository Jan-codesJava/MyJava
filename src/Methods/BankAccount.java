package Methods;

public class BankAccount {
    public static void main(String[] args) {


        String bankName = "Techtorial";
        int accountNumber;
        double balance;
        String accountHolderName;
        boolean loggedIn;
        String userName;
        String passWord;

    }
}
        // create a method that will display all of the account information
        // create a method named as deposit, it will update the balance
        // create a method named as withDraw, it will update the balance
        // create a method named as logIn, it will check if the user logged in or not, it will return true/false

        /*public void accountInfo () {

            System.out.println("Bank name: " + bankName + "\n Account number is: " + accountNumber +
                    "\nBalance is: " + balance + "\nAccount Holder's Name is: " + accountHolderName
                    + "\nLogged in: " + loggedIn);
        }

        public double deposit ( double depositAmount){

            balance += depositAmount;
            System.out.println("your new balance after deposit amount of " + depositAmount + " is " + balance);
            return balance;
        }
        public double withDraw ( double withDrawAmount){

            balance -= withDrawAmount;
            System.out.println("your new balance after withdrawal amount of " + withDrawAmount + " is " + balance);
            return balance;
        }
        public boolean logIn (String user, String pass){
            if (userName.equals(user) && passWord.equals(pass)) {
                System.out.println("You successfully logged in!");
                loggedIn = true;
            } else {
                System.out.println("Your credentials are not matching! Try again");
            }

            return loggedIn;
        }


    }
}

         */
       /* String bankName = "Techtorial";
        int accountNumber;
        double balance;
        String accountHolderName;
        boolean loggedIn;
        String userName;
        String passWord;

        //create a method that will display all of the account information
        //create a method name as deposit, it will update the balance
        //create a method name as withDraw , it will update the balance
        //create a method name as login, it will check if the user logged in or not, it will return true/false

         public void accountInfo() {

            System.out.println("Bank name: "+bankName+"\n Account number is: "+accountNumber)+
                    "\nBalance is: "+balance+"\nAccount Holder's Name is: "+accountNumber
                    + "\nLogged in: " + loggedIn);
        }
        public double deposit(double depositAmount){

            balance +=depositAmount;
            System.out.println("your new balance after deposit amount of "+depositAmount+ " is "+balance);
            return balance;
        }
        public double withDraw(double withDrawAmount){

            balance +=withDrawAmount;
            System.out.println("your new balance after withdrawal amount of "+withDrawAmount+ " is "+balance);
            return balance;
        }
        public boolean logIn(String user, String pass){
            if (userName.equals(user) && passWord.equals(pass)){
                System.out.println("You successfully loggen im!");
                loggedIn = true;
            } else{
                System.out.println("Your credentials are not matching. Try again");
            }
            return loggedIn;


        }
        public static void main(String[] args);
        bankAccount java = new BankAccount();
        java.accountInfo();
        java.accountNumber = 12345;
        java.accountHolderName = "Joe";
        java.userName = "abc";
        java.passWord = "123";
        java.balance = 100_000;

        java.accountInfo();
        java.deposit(5000);
        java.withDraw(60);
        java.logIn("abc", "123");
        java.accountInfo();
    }


    }
}

        */

