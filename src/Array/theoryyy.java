package Array;

public class theoryyy {
    public static void main(String[] args) {

        /*

        Array

        sort() ; arranges the array from smaller to larger elemnet

        binarysearch() --> gives the number of element if the element is present,
        it gives the possible position for with - (negative sign front of it)

        split();nit splits the string grom provided parameter(regex --> regular expression)

        - split method returns string array

        for each loop: --> it starts from first element and goes all the way to the end of given array.

        ------------------------------------

        multidimentional array:

        int[][] numbers = new int [size of each array][size of each array];

        numbers[0][0] = 32;

        sout(numbers[0][0]);

        sout(Arrays.deepToString(numbers)); --> it will print everything in the multidimentional array
         */

        /*
        for(int[] x : numbers){

        for (int number :x){


        for(){
        }
        }
         */
    }
}
